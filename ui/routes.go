package ui

import (
	"github.com/julienschmidt/httprouter"
	//"github.com/tc1io/wallet/repository"
	//"html/template"
)

func Register(app *httprouter.Router) {
	//parsedTemplate := template.Must(template.ParseGlob("templates/*"))
	//askListRepo := repository.NewAskListRepo()
	//clientRepo := repository.NewClientRepo()
	//app.GET("/", GetHandler(parsedTemplate, &askListRepo))
	//app.GET("/pfi", GetPfiHandler(&clientRepo))
	//app.GET("/cancel/:id", CancelAskRequest())
	//app.POST("/post", CreateAskHandler(&askListRepo, &clientRepo))
	//app.POST("/pf1", PfOneHandler)
	//app.POST("/pf2", PfTwoHandler)
	//app.POST("/pf3", PfThreeHandler)
	//app.POST("/pf4", PfFourHandler)
}
